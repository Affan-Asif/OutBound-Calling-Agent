name: voice-agent-env

channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.10
  - pip
  - pip:
      - fastapi
      - uvicorn
      - websockets
      - twilio
      - python-dotenv
      - requests